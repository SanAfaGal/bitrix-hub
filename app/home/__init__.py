"""Página de inicio del staff: la única URL que hay que memorizar.

Enlaza a `/interno/nuevo-lead` (todo el staff) y `/admin` (solo `ADMIN_EMAILS`,
tarjeta deshabilitada para el resto). HTML/CSS sueltos en `templates/`/`static/`,
mismo patrón que `app/interno/` — ver ese README para la discusión de trade-offs.
"""
